# 图像分类置信度阈值指南

## 置信度阈值说明

置信度阈值决定了哪些分类结果会被保留为标签。阈值越高，标签越准确但数量可能越少；阈值越低，标签越多但可能包含不准确的结果。

## 推荐配置

### 1. 严格模式（高准确率，推荐用于生产环境）

```yaml
smart-tag:
  classification:
    confidence-threshold: 0.7  # 70%以上
    strict-top-k: false        # 先过滤再取top-k
    top-k: 3                   # 最多3个标签
```

**特点：**
- ✅ 准确率高，误判少
- ✅ 标签质量高，用户信任度高
- ⚠️ 标签数量可能较少（1-3个）

**适用场景：**
- 需要高质量标签的场景
- 用户对准确性要求高
- 标签用于重要分类或搜索

### 2. 平衡模式（推荐默认配置）

```yaml
smart-tag:
  classification:
    confidence-threshold: 0.5  # 50%以上
    strict-top-k: false        # 先过滤再取top-k
    top-k: 5                   # 最多5个标签
```

**特点：**
- ✅ 准确率和数量平衡
- ✅ 大多数情况下能提供有用的标签
- ✅ 适合大多数场景

**适用场景：**
- 日常使用
- 需要一定数量的标签用于搜索
- 对准确率有一定要求但不需要极严格

### 3. 宽松模式（更多标签）

```yaml
smart-tag:
  classification:
    confidence-threshold: 0.3  # 30%以上
    strict-top-k: false        # 先过滤再取top-k
    top-k: 5                   # 最多5个标签
```

**特点：**
- ✅ 标签数量多
- ⚠️ 可能包含一些不准确的标签
- ⚠️ 需要用户手动筛选

**适用场景：**
- 探索性使用
- 需要大量标签用于初步分类
- 用户可以接受一定误判

### 4. 强制Top-K模式（不推荐）

```yaml
smart-tag:
  classification:
    confidence-threshold: 0.5
    strict-top-k: true         # ⚠️ 强制返回前k个
    top-k: 5
```

**特点：**
- ⚠️ 总是返回5个标签（即使置信度很低）
- ❌ 可能包含大量不准确的标签
- ❌ 准确率下降明显

**不推荐原因：**
- 如果前5个结果中有些置信度只有10-20%，这些标签基本不可信
- 会污染标签库，影响搜索和分类质量

## 置信度阈值参考

| 阈值 | 含义 | 准确率 | 标签数量 | 推荐场景 |
|------|------|--------|----------|----------|
| 0.7+ | 高置信度 | 很高 | 少（1-3个） | 生产环境、重要分类 |
| 0.5-0.7 | 中等置信度 | 较高 | 中等（3-5个） | **推荐默认** |
| 0.3-0.5 | 低置信度 | 一般 | 多（5-10个） | 探索性使用 |
| <0.3 | 很低置信度 | 低 | 很多 | 不推荐 |

## Top-K 策略对比

### strict-top-k: false（推荐）

**工作流程：**
1. 对所有1000个类别按置信度排序
2. 过滤掉置信度低于阈值的
3. 取前k个（如果还有的话）

**优点：**
- ✅ 保证标签质量
- ✅ 避免低置信度标签污染
- ✅ 准确率高

**示例：**
- 如果前5个置信度是：[0.85, 0.72, 0.68, 0.45, 0.32]
- 阈值=0.5，top-k=5
- 结果：返回3个标签（0.85, 0.72, 0.68）

### strict-top-k: true（不推荐）

**工作流程：**
1. 对所有1000个类别按置信度排序
2. 强制取前k个（忽略置信度阈值）

**缺点：**
- ❌ 可能包含低置信度标签
- ❌ 准确率下降
- ❌ 标签质量不稳定

**示例：**
- 如果前5个置信度是：[0.85, 0.72, 0.68, 0.45, 0.32]
- 阈值=0.5，top-k=5，strict-top-k=true
- 结果：返回5个标签（包括0.45和0.32这两个低置信度的）

## 实际测试建议

### 测试不同阈值

1. **先用严格模式测试：**
   ```yaml
   confidence-threshold: 0.7
   strict-top-k: false
   top-k: 3
   ```
   观察标签质量和数量

2. **如果标签太少，降低阈值：**
   ```yaml
   confidence-threshold: 0.5
   strict-top-k: false
   top-k: 5
   ```

3. **如果标签质量下降，提高阈值：**
   ```yaml
   confidence-threshold: 0.6
   strict-top-k: false
   top-k: 4
   ```

### 查看日志

日志会显示每个标签的置信度：
```
AI分类生成 3 个标签: [金鱼(85.23%), 水族馆(72.15%), 宠物(68.90%)]
```

根据日志中的置信度分布，调整阈值：
- 如果大多数标签都在0.5-0.7之间，阈值设为0.5合适
- 如果大多数标签都在0.7+，可以提高到0.6-0.7
- 如果很多标签在0.3-0.5，说明模型对这张图不太确定，建议保持0.5

## 最佳实践

1. **默认配置（推荐）：**
   ```yaml
   confidence-threshold: 0.5
   strict-top-k: false
   top-k: 5
   ```

2. **根据照片类型调整：**
   - 人像照片：可以稍微降低阈值（0.4-0.5），因为可能识别出服装、场景等
   - 风景照片：可以提高阈值（0.6-0.7），因为场景识别通常更准确
   - 物体照片：保持默认（0.5）

3. **定期检查标签质量：**
   - 查看日志中的置信度分布
   - 检查用户反馈
   - 根据实际情况调整

4. **避免强制Top-K：**
   - 除非有特殊需求，否则不要设置 `strict-top-k: true`
   - 质量比数量更重要

## 总结

**推荐配置：**
- `confidence-threshold: 0.5`（平衡准确率和数量）
- `strict-top-k: false`（保证质量）
- `top-k: 5`（适中的标签数量）

**关键原则：**
- ✅ 质量优先：宁可标签少，也不要错误的标签
- ✅ 灵活调整：根据实际效果调整阈值
- ✅ 查看日志：根据置信度分布优化配置

